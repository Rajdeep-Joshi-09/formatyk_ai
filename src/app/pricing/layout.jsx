export const metadata = {
  title: 'Pricing - Formatyk',
  description: 'Transparent pricing plans for enterprise AI solutions.',
};

export default function PricingLayout({ children }) {
  return children;
}
