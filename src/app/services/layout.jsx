export const metadata = {
  title: 'Services - Formatyk',
  description: 'Explore our predictive analytics, NLP, and computer vision services.',
};

export default function ServicesLayout({ children }) {
  return children;
}
